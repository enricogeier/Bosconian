#ifndef SETTINGS_H
#define SETTINGS_H

const int MAX_FPS = 120;
const int SCREEN_SIZE_WIDTH = 1920;
const int SCREEN_SIZE_HEIGHT = 1080;
const int PLAYER_LIVES = 3;

const int PLAYER_START_X = SCREEN_SIZE_WIDTH / 2;
const int PLAYER_START_Y = SCREEN_SIZE_HEIGHT / 2;
const float MAX_VELOCITY = 300.0f; // default: 300
const float MIN_VELOCITY = 200.0f;


#endif